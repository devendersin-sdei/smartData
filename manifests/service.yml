apiVersion: v1
kind: Service
metadata:
    name: "scripsappointment-511d"
    labels:
        app: "scripsappointment-511d"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "scripsappointment-511d"